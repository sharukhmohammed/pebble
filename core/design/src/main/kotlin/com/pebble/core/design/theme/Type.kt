package com.pebble.core.design.theme

import androidx.compose.material3.Typography

// Uses the system default typeface (Roboto Flex on Android 12+).
// Override individual styles here if a custom font is added later.
val PebbleTypography = Typography()
